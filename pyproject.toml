[tool.poetry]
name = "fyp"
version = "0.1.0"
description = ""
authors = ["Christy Chan"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"
Scrapy = "^2.5.1"
black = "^21.12b0"

[tool.poetry.dev-dependencies]
black = {version = "^21.12b0", allow-prereleases = true}

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
